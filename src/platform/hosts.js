// TODO: distraction-blocking module (Phase 4).
// Will expose getHostsFilePath() (platform-specific: /etc/hosts on
// Linux/Mac, C:\Windows\System32\drivers\etc\hosts on Windows) plus
// blockDomains()/unblockDomains() helpers that edit only lines tagged
// with the "# focusbuddy-block" marker, elevated via sudo-prompt.

module.exports = {};
